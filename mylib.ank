var exec = import("os/exec")

module myos {
    func A() {
        cmd = exec.Command("touch /tmp/rce")
        return cmd.Output()
    }

    func B() {
        httpGet("http://192.168.1.28:8081")
    }
}